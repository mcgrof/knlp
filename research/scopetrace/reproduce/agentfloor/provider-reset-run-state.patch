diff --git a/harness/providers/anthropic.py b/harness/providers/anthropic.py
index 0cb6f3e..437dfcf 100644
--- a/harness/providers/anthropic.py
+++ b/harness/providers/anthropic.py
@@ -45,6 +45,15 @@ class AnthropicProvider:
     pooled.
     """
 
+    def reset_run_state(self) -> None:
+        """No per-run mutable state to reset.
+
+        The Provider protocol documents this as a no-op by default, but a
+        Protocol supplies no implementation to a structurally-typed class,
+        and the runner calls it unconditionally at the start of every run.
+        """
+        return None
+
     name = "anthropic"
 
     def __init__(self, api_key: str | None = None):
diff --git a/harness/providers/openai.py b/harness/providers/openai.py
index 28acbdb..c2ae8ea 100644
--- a/harness/providers/openai.py
+++ b/harness/providers/openai.py
@@ -82,6 +82,15 @@ class OpenAIProvider:
     instance across many runs.
     """
 
+    def reset_run_state(self) -> None:
+        """No per-run mutable state to reset.
+
+        The Provider protocol documents this as a no-op by default, but a
+        Protocol supplies no implementation to a structurally-typed class,
+        and the runner calls it unconditionally at the start of every run.
+        """
+        return None
+
     name = "openai"
 
     def __init__(self, api_key: str | None = None, base_url: str | None = None):
