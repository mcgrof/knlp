"""Failure-conditioned revision versus resampling on math word problems.

A frozen-policy inference experiment: on problems where independent
sampling produced no verified answer, compare spending a fixed resource
budget on more independent attempts against strategies that first
revise the approach (a same-model summary of the failed attempts, a
content-free planning control, and per-candidate critique and
correction).  No training happens here.
"""
