"""Reinforcement-learning scaffolding for knlp.

The first environment is a headless build of Extreme Tux Racer; the
trainer, the run supervisor and the GPU lease are environment-agnostic
so the same pieces carry over to later tasks. See rl/README.md.
"""
